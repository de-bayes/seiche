[Unit]
Description=run Seiche PDF brief every 6 hours

[Timer]
# 00:20, 06:20, 12:20, 18:20 UTC -- a few minutes after the :14 publish so the
# data.json the brief reads is fresh
OnCalendar=00/6:20:00
Persistent=true

[Install]
WantedBy=timers.target
